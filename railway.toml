[build]
builder = "DOCKERFILE"
dockerfilePath = "Dockerfile"

[deploy]
# Liveness uniquement (pas DB/SMTP) — évite Healthcheck failure 10 min sur Railway.
healthcheckPath = "/api/actuator/health/liveness"
healthcheckTimeout = 300
restartPolicyType = "ON_FAILURE"
